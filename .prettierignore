# fork 運用: upstream hermes-agent のファイルを prettier で触ると merge コンフリクトの温床になる。
# 全ファイルを ignore し、kai 所有のパスだけを明示的に対象化する（allowlist 方式）。
*
!docs/
docs/*
!docs/kai/
!docs/kai/**
!CLAUDE.md
!.claude/
.claude/*
!.claude/agents/
!.claude/agents/**
!kai-services/
!kai-services/**
# サードパーティ（npm 依存・vendored 配布物）は対象外
kai-services/**/node_modules/
kai-services/**/vendor/
