# <コンポーネント名> 基本設計書

<!--
このテンプレートの使い方:
- <> で囲まれた箇所を置き換える。該当しないセクションは「なし」と明記して残す（削除しない — 「検討漏れ」と「該当なし」を区別するため）。
- 書き方の原則は docs/kai/design/README.md を参照。
- 1 コンポーネント = 1 ファイル。実装が 1 PR（目安: 変更 5 ファイル / 300 行）に収まらない場合は「11. 実装手順」で PR 単位に分割する。
-->

- **ステータス:** ドラフト | レビュー済み | 実装中 | 実装済み
- **作成日 / 更新日:** <YYYY-MM-DD>
- **満たす要件:** <F-xx, F-yy>（`docs/kai/requirements.md`）
- **マイルストーン:** <M0〜M5 / フェーズ n>（`docs/kai/mvp-plan.md`）
- **種別:** plugin | skill | 独立プロセス | cron ジョブ | skin | config のみ
- **配置:** <リポジトリ内パス。例: `plugins/kai_narrator/`>

## 1. 目的と責務

<このコンポーネントが「何をするか」を 2〜4 行で。>

**やらないこと（非責務）:**

- <隣接コンポーネントとの境界を明示。例: 「TTS 合成はしない（speech-player の責務）」>

## 2. 配置と Footprint Ladder

- **選んだ段:** <① 既存拡張 / ② CLI+skill / ③ service-gated tool / ④ plugin / ⑤ MCP / ⑥ 独立プロセス>
- **理由:** <なぜこの段か。1〜2 行>
- **コア改変:** なし（必須）。<もし避けられない場合は、対象ファイル・行数・upstream 追従リスクを明記し、オーナー承認を得る>

## 3. インターフェース

### 3.1 提供するインターフェース

<形式は具体的に。HTTP なら メソッド + パス + リクエスト/レスポンス JSON 例。hook なら hook 名と受け取る kwargs。CLI なら コマンドと引数。>

```
<例:
POST /say
Request:  {"text": "...", "emotion": "thinking", "role": "implement",
           "work_thread_id": "kai-agent#123", "correlation_id": "..."}
Response: 202 {"queued": true, "queue_depth": 2}
>
```

### 3.2 依存するインターフェース

| 依存先 | 形式 | 用途 | 不達時の挙動 |
| --- | --- | --- | --- |
| <例: mac-aquestalk> | HTTP | 音声合成 | <例: 3s タイムアウト → 字幕のみ縮退> |

## 4. データモデル

<永続化するもの・イベントスキーマ・設定ファイル形式。JSONL イベントは `docs/kai/design/00-system.md` の共通エンベロープに従い、payload 部分のみ定義する。>

```jsonc
<スキーマ例>
```

## 5. 処理フロー

<正常系を番号付きで。分岐・非同期境界（スレッド/プロセス/キュー）を明示。>

1. <...>
2. <...>

## 6. エラー処理・縮退

| 失敗モード | 検知 | 挙動 | 復旧 |
| --- | --- | --- | --- |
| <例: TTS 不達> | <タイムアウト 3s> | <字幕のみで継続（配信を止めない）> | <次リクエストで自動再試行> |

**原則:** optional feature の失敗はメインループ・配信を止めない（warn ログ + 縮退）。

## 7. 設定

| キー | 置き場所 | デフォルト | 説明 |
| --- | --- | --- | --- |
| <例: `kai.narrator.max_queue`> | <config.yaml / plugin 設定 / env(.env は機密のみ)> | <値> | <説明> |

## 8. セキュリティ

- **秘匿情報:** <このコンポーネントが触れる秘匿情報と、マスク/非保持の方法。配信・ログ・トレースへの漏洩経路を列挙して潰す>
- **入力の信頼境界:** <外部入力（チャット・Issue 本文等）を扱う場合、prompt injection / インジェクション対策>
- **ネットワーク:** <bind するアドレス（Tailscale 内のみ等）、公開ポートなし>

## 9. テスト・検証

- **ユニットテスト:** <対象と方針。hermes 側は `scripts/run_tests.sh` 経由>
- **runtime acceptance（必須）:** <実機での検証手順をコピペ可能なコマンドで>

```bash
<検証コマンド例>
```

## 10. 実装手順（コーディングエージェント向け）

<着手順に。各ステップは独立に検証可能な単位にする。PR を分ける場合はここで区切る。>

1. <ステップ / 作成・変更するファイルの一覧>
2. <...>

**変更してよいファイル:** <明示的な allowlist。ここに無いファイル（特にコアファイル）は変更禁止>

## 11. 完了条件（DoD チェックリスト)

- [ ] <観測可能な条件 1>
- [ ] <観測可能な条件 2>
- [ ] runtime acceptance の証跡（ログ/スクショ/配信 URL）を PR に記載
- [ ] 秘匿情報がログ・トレース・配信に漏れないことを確認
- [ ] `scripts/run_tests.sh` / `ruff check .` / `ty check` がグリーン（hermes 側コードを触った場合）

## 12. 制約・禁止事項

- コアファイル改変禁止（§2 の allowlist 外は触らない）
- プロンプトキャッシュ不可侵（system prompt のバイト安定・role 交替維持・会話途中のコンテキスト改変禁止）
- <このコンポーネント固有の禁止事項。例: 「hook 内で同期 LLM/HTTP 呼び出し禁止（enqueue のみ）」>

## 13. 未決事項

| # | 事項 | 決め方 / 期限 |
| --- | --- | --- |
| 1 | <未決事項> | <実装時に検証 / オーナー判断 等> |
