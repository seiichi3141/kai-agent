# kai 自動配信 — 基本設計（草案・作成中）

**ステータス: 検討中。** 要件 [`00-requirements.md`](./00-requirements.md) の**確定事項**
（ゴール G1〜G4・活動領域・フェイルセーフ二層防御）をアーキテクチャに落とす。要件が TBD の
部分（課題発見アルゴリズム §2・マージ待ち運用 §3・頻度尺 §6）に依存する設計は「未設計」として
明示し、憶測で固めない。

## 1. 設計原則（kai の 2 大原則 + 本機能の分離原則）

- **P-A. 会話単位のプロンプトキャッシュは不可侵**（AGENTS.md）。自動配信の制御は hermes の
  会話に割り込まない。kai への指示は既存の投入経路（`kai-ask.sh` = セッション外）を使う。
- **P-B. コアは narrow waist、機能はエッジに置く**（AGENTS.md）。`run_agent.py` 等コアは
  改変しない。自動配信は **独立プロセス（オーケストレーター）+ plugin（フェイルセーフ hook）+
  CLI（既存 broadcast/stage）** で構成する。
- **P-C. 制御と作業の分離**（要件 §7）。**配信制御は kai 本体でなく外側のオーケストレーターが
  持つ**。kai（hermes）は作業のみ。kai は自分の配信を start/stop できない（SOUL.md の禁止を
  維持し、加えて F1 hook で機械的にブロック）。暴走時も制御と監視は生存する。

## 2. 全体アーキテクチャ

```text
                ┌─────────────────────────────────────────────┐
                │  stream-orchestrator（新規・独立プロセス）    │  ← 配信制御の主体
                │  ─ ライフサイクル状態機械（§4）               │
                │  ─ preflight ゲート実行                       │
                │  ─ タスク供給（kai-ask 経由で hermes 起動）   │
                │  ─ F2 ウォッチャー（監視 → 自動 stream-stop） │
                └───┬───────────┬───────────────┬─────────────┘
       呼び出し      │           │ 起動           │ 監視（読み取り）
        （制御）     ▼           ▼               ▼
              broadcast.sh   hermes -z        speechd / kai_trace
              stage.sh       （kai 本体・作業） （発話・全事象）
                             ├ kai_ide plugin ── F1 事前ブロック hook
                             └ kai_narrator ──→ speechd → 配信画面
```

- **kai 本体（hermes）は「作業」だけ**を担い、配信の生殺与奪はオーケストレーターが握る。
- オーケストレーターは kai の**外側**にいるので、kai がハング・暴走しても
  `stream-stop` を打てる（F2 が機能する前提条件）。

## 3. コンポーネントと責務

| コンポーネント                            | 種別                                       | 責務                                               | 新規/既存                     |
| ----------------------------------------- | ------------------------------------------ | -------------------------------------------------- | ----------------------------- |
| stream-orchestrator                       | 独立プロセス（systemd）                    | ライフサイクル制御・preflight・タスク供給・F2 監視 | **新規**                      |
| フェイルセーフ hook（F1）                 | plugin（`kai_ide` 拡張 or 新 `kai_guard`） | 危険操作を実行前に block                           | **新規**                      |
| broadcast.sh / stage.sh                   | CLI                                        | OBS 配信制御・ステージ構築                         | 既存 ✅                       |
| streaming-preflight                       | チェックリスト/スクリプト                  | 開始前ゲート                                       | 既存 ✅（スクリプト化は TBD） |
| hermes（kai 本体）                        | プロセス（`-z` 起動）                      | 課題の着手・コード作業・実況生成                   | 既存 ✅                       |
| speechd / narrator / koe-bridge / overlay | 常駐サービス                               | 音声・字幕・実況                                   | 既存 ✅                       |
| kai_trace                                 | plugin                                     | 全事象 JSONL（F2 の監視データ源）                  | 既存 ✅                       |

## 4. 配信ライフサイクル（状態機械）

```text
IDLE ──(トリガー)──▶ PREFLIGHT ──(緑)──▶ STAGE ──▶ STREAMING
  ▲                     │(赤)                          │
  │                     ▼                              ▼
  │                  SKIP/通知              TASK LOOP（発見→着手→PR）
  │                                                   │
  └──── CLEANUP ◀── STREAM_STOP ◀──(終了条件/異常)────┘
```

- **トリガー**: Phase A=人間 go / Phase B 以降=systemd timer。
- **PREFLIGHT**: `streaming-preflight` を自動実行。赤なら配信せず通知（G4 の「公開は慎重」）。
- **TASK LOOP**: 1 配信で 1〜N 課題。各課題は「発見 →（Issue 起票?）→ 着手 → verify →
  PR 作成」。マージはしない（G3）。ループの供給源と歯止めは要件 §2（**未確定**）。
- **STREAM_STOP / CLEANUP**: 終了条件（尺・課題枯渇）or 異常で停止 → `streaming-preflight`
  の「配信後の後片付け」を実行。
- **F2 ウォッチャーは STREAMING 中つねに並行**し、異常時に STREAM_STOP へ遷移させる。

## 5. フェイルセーフの実装（要件 §4 の設計）

### F1. 事前ブロック（plugin hook）

- `pre_tool_call` で kai の実行前コマンド/引数を検査し、危険なら **block ディレクティブを
  返して実行を止める**（観測のみの現 narrator と異なり、ここは能動的に拒否）。
- 検査ロジックは既存の秘密パターン（`kai_narrator._SENSITIVE_RE` / `_RESULT_SECRET_RE`）を
  共有下地にし、破壊的コマンド（`git push --force`・`rm -rf`・`broadcast.sh` の
  stop/stream-\*・認証/設定書換）の denylist を足す。
- 配置: `kai_ide` は既に terminal を override しているため拡張しやすい。ただし責務が
  「配信演出」と「安全ゲート」で異なるので、**新 plugin `kai_guard` に独立させる**案を推奨
  （単一責務・テスト容易・Footprint Ladder）。**要検討**。

### F2. 独立ウォッチャー（オーケストレーター内）

- 監視データ源は **既存の副作用のない読み取り**のみ: speechd の発話状況（無音検知）、
  `kai_trace` JSONL の追記（ハング・エラー率・秘密パターンの最終防波堤）、
  Codex クォータ/トークン。
- 異常判定 → `broadcast.sh stream-stop` を実行 → CLEANUP → 人間へ通知（事後）。
- 閾値（無音 N 秒・連続失敗 M 回・クォータ上限）は設定化し初期値を持つ（要件 §4、**値は TBD**）。

## 6. 段階的実装（自律度を 1 段ずつ上げる）

| Phase                   | 到達点                                                     | この段で作るもの                                                          | 依存する未確定要件                       |
| ----------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------- | ---------------------------------------- |
| **A. 半自動**           | 人間 go で 1 本完走（preflight→配信→1 課題→停止→後片付け） | orchestrator の状態機械・F1・F2 の骨格。タスクは既存 `kai-ask` で人間投入 | なし（既存要件で作れる）                 |
| **B. スケジュール無人** | timer 起動・preflight 自動ゲート・異常自動停止が実績を積む | timer・preflight スクリプト化・通知                                       | §5 介在点・§6 頻度尺                     |
| **C. 完全無人**         | kai が課題発見・メディア運営（分析→企画）                  | 課題発見器・メディア運営（分析 API・公開承認フロー）                      | §2 課題発見・§3 マージ待ち・活動領域詳細 |

**Phase A は現行の確定要件だけで着手できる**。B/C は対応する要件（§2/§3/§5/§6）が固まってから。

## 7. 未設計（要件確定待ち・ここでは決めない）

- 課題発見器のアルゴリズムと歯止め（要件 §2）。
- マージ待ち PR の依存解決・上限（要件 §3）。
- メディア運営の API 別設計・公開承認 UI（要件 §1.5 / §5）。
- フェイルセーフ閾値の具体値（要件 §4）。
- オーケストレーターの実装言語/常駐方式（Python + systemd を有力候補として仮置き。確定は
  Phase A 着手時）。

## 8. 次アクション

1. 本設計のレビュー（オーナー / 懐疑レビュー）。
2. 要件 §2（課題発見）を詰め、Phase C の設計を追記。
3. Phase A（半自動オーケストレーター）の詳細設計 → 実装は要件・設計が固まってから。
